{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for Sortd",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:default",
    "dialog:allow-open",
    "dialog:default"
  ]
}